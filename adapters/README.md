# CapyIO Adapters

Only `mock-source` and `mock-sink` contain foundation executable behavior.
Other directories are planning boundaries with no upstream code, binary,
compatibility claim or verified license metadata.

