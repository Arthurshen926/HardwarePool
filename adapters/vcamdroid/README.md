# VCamdroid Adapter (planned)

Candidate camera vertical integration behind an isolated Connection/Projection
boundary. No upstream source, FFmpeg/RTSP component or virtual camera is present.

The crate exists in the common workspace only to reserve file ownership and the
dependency boundary. It must not be described as a working camera Adapter.

