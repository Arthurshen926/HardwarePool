# Third-party tracking

`THIRD_PARTY.yml` is the machine-readable planning/provenance index. An entry
with `source_imported: false` is not part of the distributed source. Before any
import, replace pending fields with verified upstream facts and add required
license/notice files under `LICENSES/`.

